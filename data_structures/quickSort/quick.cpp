#include <iostream>
using namespace std;

/*
void quickSort(vector<Comparable> & a, int left, int right)
{
    if(left + 10 <= right)
    {
        Comparable pivot = median3(a,left,right);
        int i = left;
        j = right -1;
    }
}
*/